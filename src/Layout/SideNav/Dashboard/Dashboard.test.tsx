import { DashboardContent } from "../../Main/DashboardContent/DashboardContent";
import { render, screen } from '@testing-library/react';


describe('Component should', () => {
    it('render', () => {
        // render(<DashboardContent />)
        const hello = true;
        expect(hello).toBe(true)
    })
 })